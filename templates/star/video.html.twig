{% extends 'base.html.twig' %}

{% block title %}Star{% endblock %}
{% block sub_title %}Video{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .content-wrapper, .content, .dark {
          background-color: #888;
        }

        .center-block {
          text-align: center;
        }
    </style>
{% endblock %}

{#
{% block javascripts %}
    {{ parent() }}
    <script>
        $(function () {
        });
    </script>
{% endblock %}
#}

{% block content %}

    <div class="center-block">
  		<h3>
			{# videoData : {
				0 = ID / Path,
				1 = Name,
				2 = Season,
				3 = Episode,
				4 = EpisodeAll,
				5 = IsExtra
			#}
  			<font id="titleFont">Staffel {{ videoData[2] }} - Episode {{ videoData[3] }}: {{ videoData[1] }}</font>
  		</h3>
    </div>

    <div class="center-block">
  		<video class="dark" width="80%" controls preload="auto">
  			<source src="{{ videoPath }}" type="video/mp4">
			{#<source src="{{ audioPath }}" type="audio/ogg">#}
  		  <p>Your browser does not support the video tag.</p>
  		</video>
    </div>

{% endblock %}
